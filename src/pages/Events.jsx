export default function Events() {
    return (
        <div style={{ padding: '2rem', textAlign: 'center' }}>
            <h1>Upcoming Events</h1>
            <p>Check out our latest runs and meetups here.</p>
        </div>
    );
}
